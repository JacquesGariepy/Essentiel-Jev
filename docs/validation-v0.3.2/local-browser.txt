{
  "status": "passed",
  "checks": [
    "Preview is explicitly synthetic; persistence starts off",
    "All ten navigation views render",
    "Opt-in persistence writes the workspace only after the user enables it",
    "Task editor creates dated recurring tasks with explicit checklists",
    "Unchecked checklist blocks completion, with no lost mutation",
    "Manual completion creates exactly one recurring successor",
    "Unfinished dependencies block UI completion",
    "Planner respects the explicitly entered capacity and reserve",
    "Focus timer starts, pauses and stops without completing tasks",
    "Workflow activation creates four editable tasks without inventing dates",
    "Domain profiles filter optional spaces without deleting their data",
    "Routine editor and explicit completion history work",
    "Money entry uses minor units; budget overrun remains a visible local warning",
    "Subscription and savings forms persist their actual entered values",
    "Meal ingredients become explicit shopping entries after confirmation",
    "Notebook persists document locations and renders embedded markup as inert text",
    "Verbatim evidence, human-only filing and confirmed-date ICS export work",
    "Linked task uses the human-confirmed date, not an unapproved prediction",
    "New source stays unanalysed; no heuristic output masquerades as Jev",
    "Legacy JSON source batches remain usable; exact duplicates skip and imported AI approvals are invalidated",
    "Decision matrix preserves manual scores and only records an explicit human choice",
    "All three primitives render; synthetic observations are excluded from calibration metrics",
    "Dependent judgments require a separate editable next-step request",
    "Sending requires explicit consent; standalone preview never makes live calls",
    "Global search finds user-created objects locally",
    "Whole-workspace JSON export contains the entered data (download transport mocked)",
    "Backup import invalidates embedded source classifications and date approvals",
    "Undo restores the previous validated workspace",
    "Opt-in save/restore round-trip preserves manual work (storage transport mocked)",
    "Empty search results do not display unrelated spaces",
    "390px mobile view fits without horizontal page overflow",
    "Language and theme switches preserve the workspace",
    "English preview includes English sample data and all 28 domains",
    "No uncaught browser JavaScript errors across all checks"
  ],
  "count": 34,
  "uncaught_errors": [],
  "limitations": [
    "Browser content injected offline; managed Chromium blocked URL navigation.",
    "localStorage and download transport are test doubles.",
    "HTTP contracts validated separately by real local Node HTTP tests with mock upstream provider.",
    "No paid Jev inference, production deployment, real provider accuracy or OS calendar-import validation.",
    "Encryption cryptography verified with Node Web Crypto, not opaque-origin browser crypto."
  ]
}
